# Tasks

## 1. Сущности в packages/entity

- [ ] 1.1 Добавить `Exercise` (name, sets, reps) и `ExerciseSet` (id со сентинелом `''`, name, список упражнений, `copyWith`, `props`) в `packages/entity/lib/src/training/` и экспортировать из `entity.dart` — проверка: `flutter analyze` без ошибок, оба класса доступны из `package:entity/entity.dart`
- [ ] 1.2 Добавить `setId` (String, дефолт `''`) в `PowerTraining` с обновлением `copyWith`, `props` и `power_training.g.dart` в детерминированном виде json_serializable (`setId: json['setId'] as String? ?? ''`) — проверка: `dart run melos run codegen` не даёт диффа в `power_training.g.dart`

## 2. Хранение и связь в базе

- [ ] 2.1 Добавить таблицу `ExerciseSetTable` (PK `id` + колонка `data` через тот же JSON-конвертер) и nullable-колонку `set_id` в `power_training_table`; поднять `schemaVersion` до 3 и добавить шаг `if (from < 3)` с `m.addColumn(...)` и `m.createTable(...)` — проверка: `database.g.dart` после codegen содержит `setId` и `exerciseSetTable`
- [ ] 2.2 Добавить `BaseExerciseSetRepository implements BaseRepository<ExerciseSet>` в `packages/storage_api` с экспортом — проверка: `flutter analyze` без ошибок
- [ ] 2.3 Реализовать `ExerciseSetRepository` в `packages/storage_local_api` по образцу `PowerTrainingRepository` (create с выдачей id, findById, findAll через `watch()`, findAllById, deleteById) + экспорт — проверка: `flutter analyze` без ошибок
- [ ] 2.4 Добавить `update(PowerTraining)` в `BasePowerTrainingRepository`, drift-реализацию (запись `set_id` из сущности вместе с `data`) и mock-реализацию — проверка: `flutter analyze` без ошибок
- [ ] 2.5 Добавить `ExerciseSetRepositoryMock` в `packages/storage_mock_api` (по образцу `PowerTrainingRepositoryMock`) + экспорт — проверка: `flutter analyze` без ошибок
- [ ] 2.6 Зарегистрировать `BaseExerciseSetRepository` в провайдерах `lib/main.dart` — проверка: приложение запускается и открывает список тренировок

## 3. Тема и локализация

- [ ] 3.1 Добавить `tertiary: 0xFFA78BFA` и `onTertiary: 0xFF1B1140` в `ColorScheme` в `lib/main.dart` — проверка: `flutter analyze` без ошибок, цвет доступен как `colorScheme.tertiary`
- [ ] 3.2 Добавить строки `startTraining`, `changeSet`, `chooseSet`, `start`, `edit`, `previousTrainings`, `exercisesCount`, `setsCount`, `durationApprox`, `moreExercises` в `app_ru.arb` и `app_en.arb` с ICU-плюралами (`one/few/many/other`) и метаданными плейсхолдеров — проверка: `flutter gen-l10n` без ошибок, ключи доступны через `AppLocalizations`

## 4. Экран тренировки (борды 09–13)

- [ ] 4.1 Создать `WorkoutBloc` (`WorkoutStarted`, `SetExpansionToggled`), состояние с тренировкой, набором, статусом и `isExpanded`, и маршрут `/workout` с аргументом `PowerTraining` — проверка: экран открывается и запрашивает набор по `training.setId`
- [ ] 4.2 Реализовать `SetCard` (шапка 64px с именем 20/600 и повёрнутым chevron, разделители, строки упражнений 48px с повторами справа) — проверка: свёрнутая карточка 64px, развёрнутая — по числу строк
- [ ] 4.3 Добавить лимит показа `kSetPreviewLimit = 3`: при большем числе упражнений показывать три строки и футер «и ещё N упражнений» с chevron, тап по футеру открывает полный список — проверка: набор из 7 упражнений показывает 3 строки и футер «и ещё 4 упражнения»
- [ ] 4.4 Реализовать сводку: карточка 312×40 радиуса 12 с `Text.rich` по центру, числа 14/600 (акцент `#48CAA0` при выбранном наборе, `#9AA3AE` без набора), подписи 13/400, разделители `#3A424E`, время как «~N мин» из расчёта 4 мин на подход с округлением до 10 — проверка: 3 упражнения × 3 подхода дают «3 упражнения · 9 подходов · ~40 мин»
- [ ] 4.5 Реализовать шапку экрана через `PreferredSize` высотой 136 (`#1C2129`): стрелка назад, имя 28/600 на y=50, описание 14 `#9AA3AE` на y=92, разделитель 1px `#3A424E` — проверка: визуально совпадает с бордами 09/13, кнопка назад возвращает на предыдущий экран
- [ ] 4.6 Реализовать полный список набора в `showModalBottomSheet` с прокруткой (имя набора, подзаголовок «N упражнений · M подходов», список строк с разделителями, без крестика и без «изменить») — проверка: длинный список прокручивается, высота шторки не растёт, закрытие свайпом/тапом вне шторки возвращает экран в прежнее состояние
- [ ] 4.7 Реализовать нижний блок кнопок 48/r24: «Предыдущие тренировки» (`secondary`), «Редактировать» (`tertiary`), «Начать» (`primary`), с `onPressed: null` и явными disabled-цветами `#2C3039`/`#72767E` — проверка: нажатия ничего не делают и приложение не падает
- [ ] 4.8 Реализовать состояние «набор не выбран»: виджет выбора набора с фоном `#1C3436`, обводкой 1px `#48CAA0` и primary-лейблом «Выбрать набор упражнений», нулевая сводка, «Начать» и «Редактировать» недоступны — проверка: при пустом `setId` (и при ссылке на несуществующий набор) экран выглядит как борд 13, карточки набора нет

## 5. Экран деталей тренировки (борд 04)

- [ ] 5.1 Добавить в `pt_details.dart` нижнюю кнопку «Начать тренировку» (48/r24, primary, отступы 16 по бокам и снизу) по макету и навигацию `pushNamed('/workout', arguments: training)` — проверка: тап открывает экран тренировки, «назад» возвращает на детали тренировки

## 6. Проверка на машине разработчика

- [ ] 6.1 Прогнать `dart run melos run codegen` и убедиться, что ручные правки `*.g.dart` не дали диффа — результат: `git diff` по `*.g.dart` пуст (кроме ожидаемых `setId`/`exerciseSetTable`), Dart в песочнице недоступен, запускает пользователь
- [ ] 6.2 Прогнать `flutter analyze` — результат: нет ошибок и новых предупреждений
- [ ] 6.3 Проверить на устройстве борд 04 → кнопку → состояние борда 13 на экране тренировки → возврат назад; убедиться, что существующие тренировки после миграции на месте с пустой ссылкой на набор — результат: данные не потеряны, навигация работает в обе стороны
